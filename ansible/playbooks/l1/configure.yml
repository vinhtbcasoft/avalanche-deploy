---
# Phase 2: Configure nodes to track and validate your L1
#
# Run this AFTER create-l1 tool has created your subnet/chain.
#
# Usage:
#   ansible-playbook playbooks/l1/configure.yml -e subnet_id=<SUBNET_ID>
#
# Or with chain config:
#   ansible-playbook playbooks/l1/configure.yml \
#     -e subnet_id=<SUBNET_ID> \
#     -e chain_id=<CHAIN_ID>

- name: Gather L1 Validator Info
  ##
  # Specifies which remote servers, virtual machines, or network devices the tasks within that play should execute against.
  # Ansible searches its inventory source to map which IP addresses or domain names belong to that group label.
  # "ansible_invetory" is an output within terraform/l1/aws output.tf file.  It is populated after "terraform apply".
  ##
  hosts: validators
  become: false
  gather_facts: false

  tasks:
    - name: Get NodeID from each validator
      ansible.builtin.uri:
        url: "http://127.0.0.1:9650/ext/info"
        method: POST
        body_format: json
        body:
          jsonrpc: "2.0"
          id: 1
          method: "info.getNodeID"
        return_content: true
      register: node_id_result
      delegate_to: "{{ inventory_hostname }}"

    - name: Store NodeID as fact
      ansible.builtin.set_fact:
        node_id: "{{ node_id_result.json.result.nodeID }}"

- name: Configure Nodes for L1 (Phase 2)
  hosts: validators:rpc
  become: true
  gather_facts: true

  vars:
    subnet_id: "{{ subnet_id }}"  # Required - pass via -e
    chain_id: "{{ chain_id | default('') }}"  # Optional
    avalanchego_network: "{{ network | default('fuji') }}"
    avalanchego_config_dir: /etc/avalanchego
    avalanchego_data_dir: /var/lib/avalanchego
    avalanchego_log_dir: /var/log/avalanchego
    avalanchego_plugin_dir: "{{ avalanchego_data_dir }}/plugins"
    avalanchego_user: avalanche
    avalanchego_group: avalanche
    avalanchego_http_port: 9650
    avalanchego_staking_port: 9651
    avalanchego_api_admin_enabled: false
    avalanchego_api_info_enabled: true
    avalanchego_api_health_enabled: true
    avalanchego_log_level: info
    avalanchego_log_display_level: info
    avalanchego_partial_sync_primary_network: true
    avalanchego_network_ids:
      fuji: 5
      mainnet: 1
    # Determine node type from inventory group
    avalanchego_node_type: "{{ 'rpc' if 'rpc' in group_names else 'validator' }}"
    avalanchego_index_enabled: "{{ true if avalanchego_node_type == 'rpc' else false }}"

  pre_tasks:
    - name: Validate subnet_id is provided
      ansible.builtin.fail:
        msg: "subnet_id is required. Run with: -e subnet_id=<YOUR_SUBNET_ID>"
      when: subnet_id is not defined or subnet_id == ""

    - name: Build L1 bootstrap lists
      ansible.builtin.set_fact:
        l1_bootstrap_ids: "{{ groups['validators'] | map('extract', hostvars, 'node_id') | list }}"
        l1_bootstrap_ips: "{{ groups['validators'] | map('extract', hostvars, 'ansible_host') | map('regex_replace', '$', ':9651') | list }}"

  tasks:
    - name: Load custom node config from repository config directory
      ansible.builtin.set_fact:
        avalanchego_custom_node_config: >-
          {{
            lookup('file', playbook_dir + '/../../../configs/l1/node/' + avalanchego_node_type + '-node-config.json')
            | from_json
          }}
      failed_when: false

    - name: Build base node configuration
      ansible.builtin.set_fact:
        avalanchego_base_node_config:
          network-id: "{{ avalanchego_network_ids[avalanchego_network] }}"
          http-host: "0.0.0.0"
          http-port: "{{ avalanchego_http_port }}"
          staking-port: "{{ avalanchego_staking_port }}"
          http-allowed-hosts: "*"
          api-admin-enabled: "{{ avalanchego_api_admin_enabled }}"
          api-info-enabled: "{{ avalanchego_api_info_enabled }}"
          api-health-enabled: "{{ avalanchego_api_health_enabled }}"
          index-enabled: "{{ avalanchego_index_enabled }}"
          log-level: "{{ avalanchego_log_level }}"
          log-display-level: "{{ avalanchego_log_display_level }}"
          log-dir: "{{ avalanchego_log_dir }}"
          db-dir: "{{ avalanchego_data_dir }}/db"
          plugin-dir: "{{ avalanchego_plugin_dir }}"
          chain-config-dir: "{{ avalanchego_config_dir }}/chains"
          partial-sync-primary-network: "{{ avalanchego_partial_sync_primary_network }}"
          track-subnets: "{{ subnet_id }}"

    - name: Add L1 bootstrap info to config
      vars:
        bootstrap_config:
          bootstrap-ids: "{{ l1_bootstrap_ids | join(',') }}"
          bootstrap-ips: "{{ l1_bootstrap_ips | join(',') }}"
      ansible.builtin.set_fact:
        avalanchego_base_node_config: "{{ avalanchego_base_node_config | combine(bootstrap_config) }}"
      when: l1_bootstrap_ids is defined and l1_bootstrap_ids | length > 0

    - name: Merge custom config with base config
      ansible.builtin.set_fact:
        avalanchego_final_node_config: "{{ avalanchego_base_node_config | combine(avalanchego_custom_node_config | default({})) }}"

    - name: Write node configuration
      ansible.builtin.copy:
        content: "{{ avalanchego_final_node_config | to_nice_json }}"
        dest: "{{ avalanchego_config_dir }}/node.json"
        owner: "{{ avalanchego_user }}"
        group: "{{ avalanchego_group }}"
        mode: "0644"
      notify: Restart avalanchego

    - name: Create chain config directory for L1
      ansible.builtin.file:
        path: "{{ avalanchego_config_dir }}/chains/{{ chain_id }}"
        state: directory
        owner: "{{ avalanchego_user }}"
        group: "{{ avalanchego_group }}"
        mode: "0755"
      when: chain_id != ""

    - name: Determine chain config file for RPC nodes
      ansible.builtin.set_fact:
        chain_config_file: >-
          {%- if avalanchego_node_type == 'rpc' and rpc_type is defined -%}
            rpc-{{ rpc_type }}-chain-config.json
          {%- else -%}
            {{ avalanchego_node_type }}-chain-config.json
          {%- endif -%}
      when: chain_id != ""

    - name: Check if specific chain config exists
      ansible.builtin.stat:
        path: "{{ playbook_dir }}/../../../configs/l1/chain/{{ chain_config_file | trim }}"
      register: specific_config
      delegate_to: localhost
      become: false
      when: chain_id != ""

    - name: Fallback to generic node type config if specific doesn't exist
      ansible.builtin.set_fact:
        chain_config_file: "{{ avalanchego_node_type }}-chain-config.json"
      when: chain_id != "" and specific_config.stat is defined and not specific_config.stat.exists

    - name: Copy chain config (validator or RPC specific)
      ansible.builtin.copy:
        src: "{{ playbook_dir }}/../../../configs/l1/chain/{{ chain_config_file | trim }}"
        dest: "{{ avalanchego_config_dir }}/chains/{{ chain_id }}/config.json"
        owner: "{{ avalanchego_user }}"
        group: "{{ avalanchego_group }}"
        mode: "0644"
      when: chain_id != ""
      notify: Restart avalanchego

  handlers:
    - name: Restart avalanchego
      ansible.builtin.systemd:
        name: avalanchego
        state: restarted

  post_tasks:
    - name: Wait for node API to come back after restart
      ansible.builtin.uri:
        url: "http://127.0.0.1:9650/ext/info"
        method: POST
        body_format: json
        body:
          jsonrpc: "2.0"
          id: 1
          method: "info.getNodeID"
        return_content: true
      register: node_check
      until: node_check.status == 200
      retries: 30
      delay: 5

    - name: Read avalanchego service state
      ansible.builtin.systemd:
        name: avalanchego
      register: configure_service_state

    - name: Assert avalanchego came back after restart
      ansible.builtin.assert:
        that:
          - configure_service_state.status.ActiveState == 'active'
        fail_msg: >-
          avalanchego is {{ configure_service_state.status.ActiveState | default('unknown') }}
          on {{ inventory_hostname }} after applying the track-subnets config -
          check 'journalctl -u avalanchego'

    # Hard requirement: the node must actually be tracking the L1. The chain
    # RPC only answers once the node tracks the subnet and the chain has
    # bootstrapped, so this fails red if tracking did not take effect.
    - name: Wait for L1 chain to be ready (node tracking the subnet)
      ansible.builtin.uri:
        url: "http://127.0.0.1:9650/ext/bc/{{ chain_id }}/rpc"
        method: POST
        body_format: json
        body:
          jsonrpc: "2.0"
          id: 1
          method: "eth_chainId"
          params: []
        return_content: true
      register: l1_check
      until: l1_check.status == 200 and (l1_check.json.result is defined)
      retries: 60
      delay: 5
      when: chain_id != ""

    - name: Note that tracking cannot be fully verified without chain_id
      ansible.builtin.debug:
        msg: >-
          chain_id was not provided - verified the node restarted and the API
          is back, but cannot verify the L1 chain RPC. Re-run with
          -e chain_id=<CHAIN_ID> for a full tracking check.
      when: chain_id == ""

    - name: Display status
      ansible.builtin.debug:
        msg: "{{ inventory_hostname }} is now tracking subnet {{ subnet_id }}{% if chain_id != '' %} (L1 chain ready){% endif %}"

- name: Auto-detect EVM chain ID and build eRPC upstreams
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Skip eRPC block when skip_erpc is set
      ansible.builtin.set_fact:
        erpc_skipped: true
      when: skip_erpc | default(false) | bool

    - name: Check for genesis file
      ansible.builtin.stat:
        path: "{{ playbook_dir }}/../../../configs/l1/genesis/genesis.json"
      register: genesis_file
      when: not (skip_erpc | default(false) | bool)

    - name: Read genesis and extract EVM chain ID
      ansible.builtin.set_fact:
        erpc_evm_chain_id: "{{ (lookup('file', playbook_dir + '/../../../configs/l1/genesis/genesis.json') | from_json).config.chainId }}"
      when:
        - not (skip_erpc | default(false) | bool)
        - genesis_file.stat.exists | default(false)
      failed_when: false

    - name: Skip eRPC if EVM chain ID not found
      ansible.builtin.debug:
        msg: "Skipping eRPC: could not read config.chainId from genesis.json"
      when:
        - not (skip_erpc | default(false) | bool)
        - erpc_evm_chain_id is not defined or erpc_evm_chain_id == ''

    - name: Mark eRPC as skipped when no chain ID
      ansible.builtin.set_fact:
        erpc_skipped: true
      when:
        - not (skip_erpc | default(false) | bool)
        - erpc_evm_chain_id is not defined or erpc_evm_chain_id == ''

    - name: Check for RPC groups
      ansible.builtin.set_fact:
        has_rpc_nodes: >-
          {{ (groups['rpc_archive'] | default([]) | length > 0) or
             (groups['rpc_pruned'] | default([]) | length > 0) or
             (groups['rpc'] | default([]) | length > 0) }}
      when: not (erpc_skipped | default(false) | bool)

    - name: Skip eRPC if no RPC groups found
      ansible.builtin.debug:
        msg: "Skipping eRPC: no rpc_archive, rpc_pruned, or rpc groups in inventory"
      when:
        - not (erpc_skipped | default(false) | bool)
        - not (has_rpc_nodes | default(false) | bool)

    - name: Mark eRPC as skipped when no RPC nodes
      ansible.builtin.set_fact:
        erpc_skipped: true
      when:
        - not (erpc_skipped | default(false) | bool)
        - not (has_rpc_nodes | default(false) | bool)

    - name: Build archive upstream endpoints
      ansible.builtin.set_fact:
        erpc_upstreams: >-
          {{ erpc_upstreams | default([]) + [{
            'id': 'rpc-archive-' ~ (idx + 1),
            'endpoint': 'http://' ~ hostvars[item]['ansible_host'] ~ ':9650/ext/bc/' ~ chain_id ~ '/rpc',
            'rpc_type': 'archive'
          }] }}
      loop: "{{ groups['rpc_archive'] | default([]) }}"
      loop_control:
        index_var: idx
      when:
        - not (erpc_skipped | default(false) | bool)
        - groups['rpc_archive'] is defined

    - name: Build pruned upstream endpoints
      ansible.builtin.set_fact:
        erpc_upstreams: >-
          {{ erpc_upstreams | default([]) + [{
            'id': 'rpc-pruned-' ~ (idx + 1),
            'endpoint': 'http://' ~ hostvars[item]['ansible_host'] ~ ':9650/ext/bc/' ~ chain_id ~ '/rpc',
            'rpc_type': 'pruned'
          }] }}
      loop: "{{ groups['rpc_pruned'] | default([]) }}"
      loop_control:
        index_var: idx
      when:
        - not (erpc_skipped | default(false) | bool)
        - groups['rpc_pruned'] is defined

    - name: Fallback - build upstream endpoints from generic rpc group
      ansible.builtin.set_fact:
        erpc_upstreams: >-
          {{ erpc_upstreams | default([]) + [{
            'id': 'rpc-' ~ (idx + 1),
            'endpoint': 'http://' ~ hostvars[item]['ansible_host'] ~ ':9650/ext/bc/' ~ chain_id ~ '/rpc',
            'rpc_type': hostvars[item]['rpc_type'] | default('archive')
          }] }}
      loop: "{{ groups['rpc'] | default([]) }}"
      loop_control:
        index_var: idx
      when:
        - not (erpc_skipped | default(false) | bool)
        - (groups['rpc_archive'] is not defined or groups['rpc_archive'] | length == 0)
        - (groups['rpc_pruned'] is not defined or groups['rpc_pruned'] | length == 0)

    - name: Select eRPC deployment host
      ansible.builtin.set_fact:
        erpc_deploy_host: "{{ 'monitoring[0]' if (groups.get('monitoring', []) | length) > 0 else 'rpc[0]' }}"
      when: not (erpc_skipped | default(false) | bool)

- name: Deploy eRPC
  hosts: monitoring[0]:rpc[0]
  become: true
  gather_facts: true

  pre_tasks:
    - name: Skip if eRPC deployment not needed
      ansible.builtin.meta: end_play
      when: hostvars['localhost']['erpc_skipped'] | default(false) | bool

    - name: Determine whether this host should run eRPC deployment
      ansible.builtin.set_fact:
        deploy_erpc_here: "{{ inventory_hostname == (groups['monitoring'][0] if (groups.get('monitoring', []) | length) > 0 else groups['rpc'][0]) }}"

    - name: Wait for system to become reachable
      ansible.builtin.wait_for_connection:
        timeout: 300
      when: deploy_erpc_here

  roles:
    - role: erpc
      when: deploy_erpc_here
      vars:
        erpc_chain_id: "{{ hostvars['localhost']['chain_id'] }}"
        erpc_evm_chain_id: "{{ hostvars['localhost']['erpc_evm_chain_id'] }}"
        erpc_upstreams: "{{ hostvars['localhost']['erpc_upstreams'] }}"

- name: Display completion message
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Get eRPC host
      ansible.builtin.set_fact:
        erpc_host: >-
          {{ hostvars[groups['monitoring'][0]]['ansible_host']
            if (groups.get('monitoring', []) | length) > 0
            else hostvars[groups['rpc'][0]]['ansible_host'] }}
      when: not (erpc_skipped | default(false) | bool)

    - name: Show L1 info
      ansible.builtin.debug:
        msg: |

          ============================================
          Phase 2 Complete - L1 Configured
          ============================================

          Subnet ID: {{ subnet_id }}
          {% if chain_id is defined and chain_id != '' %}
          Chain ID:  {{ chain_id }}
          {% endif %}

          Your L1 validators are now running!

          Bootstrap IDs configured for validator discovery.

          RPC Endpoints:
          {% for host in groups['rpc'] | default([]) %}
            http://{{ hostvars[host]['ansible_host'] }}:9650/ext/bc/{{ chain_id }}/rpc (RPC node - recommended)
          {% endfor %}
          {% for host in groups['validators'] | default([]) %}
            http://{{ hostvars[host]['ansible_host'] }}:9650/ext/bc/{{ chain_id }}/rpc (validator - SSH tunnel only)
          {% endfor %}
          {% if not (erpc_skipped | default(false) | bool) %}

          eRPC Load Balancer (recommended for dApps):
            http://{{ erpc_host }}:4000
          eRPC Health Check:
            http://{{ erpc_host }}:4000/healthcheck
          {% endif %}

          ============================================
